<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap"
      rel="stylesheet"
    />
    <title>Gig Getter | Location</title>
  </head>
  <style>
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
      font-family: "Poppins", sans-serif;
    }
    .global-container {
      background-color: #e9ecef;
      height: 100vh;
      width: 100vw;
      display: flex;
      justify-content: center;
      align-items: center;
    }
    .card {
      background-color: white;
      outline: #ced4da solid 1px;
      min-width: 330px;
      color: #212529;
    }
    .card-body {
      padding: 3rem 2rem;
    }
    .card-title {
      text-align: center;
    }
    .error-message {
      text-align: center;
      margin-top: 1rem;
      background-color: #f8d7d9;
      padding: 0.4rem 0.8rem;
      outline: 1px solid #fc8890;
      color: #9c2c39;
    }
    form {
      margin-top: 2rem;
      display: flex;
      flex-direction: column;
      gap: 1rem;
    }
    label {
      font-size: 0.8rem;
      font-weight: 500;
    }
    input,
    select {
      width: 100%;
      border: 1px solid #adb5bd;
      padding: 0.2rem 0.4rem;
    }
    .green-outline {
      border: 3px solid #18a261 !important;
    }
    a {
      color: #0069d9;
      text-decoration: none;
    }
    a:hover {
      text-decoration: underline;
    }
    button {
      background-color: #0069d9;
      color: white;
      padding: 0.4rem 0.4rem;
      border: none;
      margin-top: 0.8rem;
      cursor: pointer;
      transition: 0.15s ease-in-out all;
    }
    button:hover {
      background-color: #015bbb;
    }
    button:focus,
    input:focus,
    select:focus {
      box-shadow: 0 0 10px #74c0fc;
      border: 1px solid #74c0fc;
      outline: none;
    }
    button:disabled {
      cursor: default;
      opacity: 0.5;
    }
    button:disabled:hover {
      background-color: #0069d9;
    }
    .focus-red:focus {
      box-shadow: 0 0 10px #e0283d !important;
      border: 1px solid #e0283d !important;
      outline: none !important;
    }
    a:focus {
      box-shadow: 0 0 10px #74c0fc;
      border: 1px solid #74c0fc;
      outline: none;
      padding: 0.3rem;
    }
    .pass-length {
      font-size: 0.8rem;
      color: #e0283d;
      margin-top: -1rem;
      text-align: center;
    }
    .sign-in {
      font-size: 0.8rem;
      text-align: center;
    }

  
    .center {
    margin: 0;
    position:relative ;
    top: 50%;
    left: 50%;
    -ms-transform: translate(-50%, -50%);
    transform: translate(-50%, -50%);
    }

  </style>

  <body>
    <div class="global-container">
      <div class="card">
        <div class="card-body">
          <h3 class="card-title">Select your choice</h3>
          <div class="card-text">
            <!-- <% if (message) {%>
            <div class="error-message"><%= message %></div>
            <% } %> -->

            <form action="/location" method="post">
           
              <div class="form-group">
                <label for="inputLocation">Location</label>
                <select id="inputLocation" name="location">
                  <% for (var i = 0; i < locations.length; i++) { %>
                  <option value="<%= locations[i].location_id %>">
                    <%= locations[i].location_name %>
                  </option>
                  <% } %>
                </select>
              </div>
              <br>
              <a href="/marketplace"
              ><button type="submit" class="center"> Search </button></a>


            </form>
          </div>
        </div>
      </div>
    </div>
  </body>
  <!-- <script src="/script/location.js"></script> -->
</html>
